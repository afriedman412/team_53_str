[tool.black]
line-length = 100
target-version = ["py312"]

[tool.ruff]
line-length = 100
target-version = "py312"
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.isort]  # only needed if you use isort separately
profile = "black"
line_length = 100
py_version = 313
